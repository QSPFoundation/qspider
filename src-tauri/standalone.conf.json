{
  "build": {
    "distDir": "../dist/apps/player-standalone",
    "devPath": "http://localhost:4200",
    "beforeDevCommand": "npm run serve:standalone",
    "beforeBuildCommand": "npm run build:standalone",
    "withGlobalTauri": false
  }
}
