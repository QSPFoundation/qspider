import styled from '@emotion/styled';

import { Right, Left, Bottom, Top } from 'react-spaces';

export const RightDock = styled(Right)`
  padding-left: 3px;
`;

export const LeftDock = styled(Left)`
  padding-right: 3px;
`;

export const BottomDock = styled(Bottom)`
  padding-top: 3px;
`;

export const TopDock = styled(Top)`
  padding-bottom: 3px;
`;
