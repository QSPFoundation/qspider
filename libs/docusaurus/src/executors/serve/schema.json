{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Dev mode for docusaurus",
  "description": "",
  "type": "object",
  "properties": {
    "port": {
      "type": "number",
      "description": "Use specified port.",
      "default": 3001
    },
    "host": {
      "type": "string",
      "description": "Use specified host.",
      "default": "localhost"
    },
    "hotOnly": {
      "type": "boolean",
      "description": "Do not fallback to page refresh if hot reload fails.",
      "default": false
    },
    "open": {
      "type": "boolean",
      "description": "Open page in the browser.",
      "default": false
    }
  },
  "required": []
}
