{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Build executor",
  "description": "",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "The full path for the new output directory, relative to the current workspace."
    },
    "minify": {
      "type": "boolean",
      "description": "Build website minimizing JS bundles.",
      "default": true
    }
  },
  "required": ["outputPath"]
}
